# =====================================================================
# LOCAL POC SETUP — uses Google AI Studio (free key) + local SQLite.
# Get a key at: https://aistudio.google.com/app/apikey
# =====================================================================

GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=AIzaSyCGw1Qy22OaGBNaghNh6UofNw5oTxpqEwQ

# --- Local ticket store (SQLite file, auto-created) ---
SQLITE_PATH=tickets.db

# --- Server ---
HOST=127.0.0.1
PORT=8000
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174

# =====================================================================
# (Optional) Vertex AI path — switch to this later for Cloud Run deploy.
# Requires `gcloud auth application-default login` and aiplatform API enabled.
# =====================================================================
# GOOGLE_GENAI_USE_VERTEXAI=TRUE
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
